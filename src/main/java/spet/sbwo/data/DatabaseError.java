package spet.sbwo.data;

public enum DatabaseError {
    CONSTRAINT_VIOLATED,
    CONCURRENT_ACCESS,
    CONNECTION_ERROR,
    DIVISION_BY_ZERO,
    INVALID_VALUE,
    STRUCTURE_ERROR,
    AUTHORIZATION_ERROR,
    OTHER
}
